export class PaintingsParameters {
  success: boolean;
  errorMessage: string;
  minPrice: number;
  maxPrice: number;
  minWidth: number;
  maxWidth: number;
  minHeight: number;
  maxHeight: number;
}